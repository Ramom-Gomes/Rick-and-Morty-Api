export type RickandMorty = {
    image: string;
    name: string;
    status: string;
    species: string;
    location : {
        name: string;
    }
}

export type Next = {
    next: string;
    prev: string;
}